import MobileBar from './mobileBar';
import DesktopBar from './desktopBar';

const NavBar = () => {
    return (
        <div>
            <MobileBar />
            <DesktopBar />
        </div>
    );
};

export default NavBar;
